<% if @label %>
  <%= tag.label @label, for: @input_attributes[:id], class: "block text-sm font-medium text-gray-600 dark:text-gray-300" %>
<% end %>

<%= tag.input **@input_attributes.merge(class: "text-gray-600 dark:text-gray-200 mt-1 p-2 block w-full rounded-md bg-gray-100 dark:bg-gray-600 border-gray-300 dark:border-gray-700 focus:ring focus:ring-indigo-200 focus:border-indigo-300")
%>
